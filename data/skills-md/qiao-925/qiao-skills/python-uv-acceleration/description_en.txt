By default, use uv instead of pip for dependency management and virtual environment creation in Python projects, boosting installation speed by 10–100×. Apply when handling Python projects, creating virtual environments, installing dependencies, or when users mention pip/venv/virtualenv.
