Interactive forms with input fields and buttons for bots. Use when you need to: display a form, handle form submission, validate form fields, create a modal window, survey employees. Do NOT use for: regular buttons in messages (→ pachca-messages), bot settings (→ pachca-bots).
