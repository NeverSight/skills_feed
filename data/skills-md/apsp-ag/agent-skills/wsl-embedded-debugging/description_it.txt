Esegui flussi di lavoro di build/flash/debug incorporati da WSL richiamando file eseguibili Windows che possiedono l'accesso USB/JTAG (ad esempio `cargo.exe`, `probe-rs.exe` o `openocd.exe`). Da utilizzare quando un agente deve riprodurre il comportamento del firmware sull'hardware, eseguire il flashing di un MCU, raccogliere log di runtime limitati o eseguire il debug della connettività del sonda da un'area di lavoro WSL.
