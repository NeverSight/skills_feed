Refaktorieren und überprüfen Sie SwiftUI-Ansichtsdateien auf konsistente Struktur, Abhängigkeitsinjektion und Beobachtungsnutzung. Verwenden Sie diese Option, wenn Sie dazu aufgefordert werden, das Layout/die Reihenfolge einer SwiftUI-Ansicht zu bereinigen, Ansichtsmodelle sicher zu handhaben (wenn möglich nicht optional) oder zu standardisieren, wie Abhängigkeiten und @Observable-Status initialisiert und übergeben werden.
