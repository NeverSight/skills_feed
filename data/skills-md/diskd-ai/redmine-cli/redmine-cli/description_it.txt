Gestisci i problemi/ticket di Redmine dalla riga di comando tramite l'API REST di Redmine utilizzando un'interfaccia in stile "red-cli". Utilizza `~/.red/config.json` per l'autenticazione (server + chiave API) con override locali opzionali in `./.red/config.json`. Da utilizzare quando è necessario elencare problemi/progetti, visualizzare problemi (con diari), aggiungere note/commenti di diario (pubblici o privati), aggiornare la descrizione del problema, aggiornare/rimuovere commenti esistenti (diari) per ID di giornale, filtrare/ordinare/impaginare elenchi o risolvere i problemi di configurazione della CLI di Redmine (id-progetto/id-utente, multi-istanza `--rid`). Trigger su Redmine, red-cli, elenco di problemi/vista/nota/modifica/commento, `--rid`, `.red/config.json`, `api-key`, `project-id`, `user-id` o richieste per automatizzare Redmine da una CLI.
