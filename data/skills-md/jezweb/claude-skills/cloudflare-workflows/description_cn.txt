使用 Cloudflare Workflows 构建持久的工作流程（GA 2025 年 4 月）。具有 step.do、step.sleep、waitForEvent、Vitest 测试、自动重试以及长时间运行任务的状态持久性。防止 12 个记录错误。

使用场合：创建工作流、实施重试或对 NonRetryableError、I/O 上下文、序列化错误、waitForEvent 超时、getPlatformProxy 故障进行故障排除。
