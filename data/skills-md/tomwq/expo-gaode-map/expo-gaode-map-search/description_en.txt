Amap Native Search High-Performance Solution: implemented via the native SDK, no web quota limits; provides full-scenario POI search (keyword/nearby/along-route/polygon/ID details); supports input hints (Autocomplete) and reverse geocoding (coordinates to address); includes complete TypeScript definitions; requires initialization with the core or navigation package.
