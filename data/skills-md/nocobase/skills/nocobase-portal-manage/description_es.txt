NocoBase 2 únicamente; nunca lo use en un proyecto NocoBase 3. ENTRADA PRIMARIA. Distribuidor predeterminado para cada solicitud de creación de UI de NocoBase, incluidas páginas, menús, bloques, campos, acciones, diseños, reacciones, interfaces KPI, paneles y gráficos, incluso cuando el usuario no menciona un Portal. Resolver un Portal habilitado a partir de la salida estructurada `nb portal list -j` antes de cargar cualquier habilidad de implementación de UI posterior: `nombre` exacto cuando se especifica; de lo contrario, no se detiene, exactamente uno es automático y varios Portales requieren una selección explícita incluso cuando uno tiene "isDefault: true"; muestra el marcador predeterminado solo como informativo. Lea el tipo sólo desde `portalType`; Invocar explícitamente `nocobase-ui-builder` solo para un Portal sin código resuelto y requerir `nocobase-ai-builder` para el desarrollo del código fuente del Portal AI. Utilice también comandos directos `nb portal` para tareas explícitas de ciclo de vida, almacenamiento de origen, sincronización, implementación, diagnóstico y destrucción del portal.
