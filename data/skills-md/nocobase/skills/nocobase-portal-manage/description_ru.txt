только NocoBase 2; никогда не используйте в проекте NocoBase 3. ПЕРВИЧНЫЙ ВХОД. Диспетчер по умолчанию для каждого запроса на создание пользовательского интерфейса NocoBase, включая страницы, меню, блоки, поля, действия, макеты, реакции, интерфейсы KPI, информационные панели и диаграммы, даже если пользователь не упоминает портал. Разрешите включенный портал из структурированного вывода `nb portal list -j` перед загрузкой любого последующего навыка реализации пользовательского интерфейса: точное `name`, если указано; в противном случае ноль останавливается, автоматическим является только один, и несколько Порталов требуют явного выбора, даже если у одного есть `isDefault: true`; показывать маркер по умолчанию только в информационных целях. Считывайте тип только из «portalType»; явно вызывайте `nocobase-ui-builder` только для решенного портала без кода и требуйте `nocobase-ai-builder` для разработки исходного кода AI Portal. Также используйте прямые команды `nbportal` для явного жизненного цикла портала, хранения исходного кода, синхронизации, развертывания, диагностики и уничтожения задач.
