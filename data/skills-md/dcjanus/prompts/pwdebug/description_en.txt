Used in scenarios where you need to operate a real browser instance via the command line for frontend debugging (such as navigation, executing JS, taking screenshots, element picking, console logs) and want to reuse the same browser session across multiple commands.
