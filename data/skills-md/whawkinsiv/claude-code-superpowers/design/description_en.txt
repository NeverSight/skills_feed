Use this skill when the user needs to create a design system, establish UI patterns, choose colors and typography, or ensure visual consistency across their SaaS app. Operates in CREATE mode (new apps), MODIFY mode (updating existing systems), or WORK WITHIN mode (building features that follow established patterns).
