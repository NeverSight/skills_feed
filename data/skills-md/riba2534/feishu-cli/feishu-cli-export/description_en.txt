Export Feishu documents or knowledge-base documents as Markdown files. Used when the user requests "export document", "convert to Markdown", or "save as md". Markdown is stored as an intermediate format in the /tmp directory.
