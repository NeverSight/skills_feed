Knowledge base operations. Used when users request to view the knowledge base, list knowledge spaces, or retrieve knowledge base nodes. Markdown is stored as an intermediate format in the /tmp directory.
