管理 duoduo 的閒置自動壓縮（idle auto‑compact）於頻道會話——依對話啟用或停用，讀取測量的壓縮統計，並在擁有者更換模型／後端或詢問閒置會話成本時，使用盈虧平衡公式重新調整閾值。當擁有者提及「自动压缩」／「smart compaction」／「压缩策略」、詢問為何會話被壓縮（出現壓縮通知）、將會話切換至計量或短快取 TTL 後端，或要求保護對話免於壓縮時，即觸發。亦在您管理的會話首次自動壓縮後主動使用——該事件攜帶校準測量值。

請始終以使用者的語言回應。
