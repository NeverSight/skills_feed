Crea un contesto di sessione conciso estraendo le informazioni chiave dai log del progetto (CLAUDE.md, TASK_BOARD.md, MIGRATION_LOG.md, DEBUG_LOG.md, GIT_LOG.md, SKILLS_LOG.md) per fornire uno stato del progetto efficiente in termini di token. Da utilizzare quando si inizia a lavorare, si ritorna dopo una pausa o si ha bisogno di una rapida panoramica del progetto senza leggere file interi.
