Use Chrome CDP to scrape any URL and convert it to Markdown. Supports two modes—automatic capture after page load or wait for user signal (for pages requiring login). Use when the user asks to save a webpage as Markdown.
