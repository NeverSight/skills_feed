由 Firecrawl API 提供支持的综合网络抓取、爬行和数据提取工具包。
提供单页抓取（scrape.py）、网页搜索（search.py）的脚本，
URL发现(map.py)、多页面爬取(crawl.py)、结构化数据提取(extract.py)、
和自主数据收集（agent.py）。
当您需要执行以下操作时使用：(1) 从网页中提取内容，(2) 搜索和抓取网络，
(3) 发现网站上的 URL，(4) 抓取多个页面，(5) 使用 JSON 模式提取结构化数据，
(6) 从网络上的任何地方自主收集数据。
需要 FIRECRAWL_API_KEY 环境变量。
