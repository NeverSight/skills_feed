Avvia lo stack di sviluppo OpenWork tramite Docker e verifica i flussi utente reali tramite Chrome MCP.

Si attiva quando l'utente menziona:
- "dev-up.sh"
- "stack di sviluppo docker"
- "verifica in chrome mcp"
- "testare il flusso reale"
