Generates the MixSeek orchestrator configuration file (orchestrator.toml). Use it for requests such as "Configure orchestrator", "Team competition settings", "Make multiple teams compete", or "Multi-team execution settings". It defines a setup that runs multiple teams in parallel and selects the best result.
