Use when the product-requirement Spec process in sdlc-dev needs to execute R3 (prototype generation) based on requirements/prd.md to produce requirements/prototype.md (task flow + page structure + ASCII wireframe + AC mapping + walk-through script), and avoid continuing generation without context/missing PRD, replacing verification checklist with Open Questions, or using non-ASCII formats that make the prototype untraceable and unreviewable.
