Use when you need to perform Discovery (reverse engineering) on an existing code base and persist the repository facts into `.aisdlc/project/`, and you notice that the AI/team keeps guessing entry points, boundaries, duplicating indexes and details, or lacking evidence chains, leading to repeated rework.
