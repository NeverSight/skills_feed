Use when you need to execute I1 (Implementation Plan) in the sdlc-dev Spec Pack, converting requirements/design into `{FEATURE_DIR}/implementation/plan.md` (the single source of truth for checklist and status), and provide an unambiguous task list for subsequent I2 execution.
