Use when the front-end code needs to produce evidence in the form of "page inventory → feature points → business processes → business rules," and the project has multiple, non-unique routing entry points, complex dynamic routing/permissions/backend menus, and the team is prone to making assumptions or missing evidence chains.
