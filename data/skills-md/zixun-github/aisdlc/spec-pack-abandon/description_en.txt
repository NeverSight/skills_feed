Use when the current Spec Pack must be abandoned/revoked due to a major requirement issue, and the corresponding `.aisdlc/specs/{branch}` directory and local/remote branches must be deleted. Before deletion, output a deletion list and require user confirmation.
