Use when you need to generate or update `{FEATURE_DIR}/verification/suites.md` (test suites) during the Spec Pack verification phase, organizing test cases into executable sets and defining blocking rules and execution order.
