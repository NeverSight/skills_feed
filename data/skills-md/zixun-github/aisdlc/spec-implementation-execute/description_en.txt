Use when you need to perform I2 (implementation execution) in the sdlc-dev Spec Pack, using `{FEATURE_DIR}/implementation/plan.md` as the single SSOT for phased implementation, running minimal validation, writing back audit information, and reporting at batch checkpoints; stop immediately upon encountering blockers or clarification items.
