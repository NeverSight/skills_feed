Use when you need to initialize a new Spec Pack in this repository’s AI SDLC process (creating a three-digit branch and the `.aisdlc/specs/{num}-{short-name}` directory), or when unsure about input parsing, short-name rules, UTF-8 BOM file path arguments, script invocation, or outputs during `spec-init`.
