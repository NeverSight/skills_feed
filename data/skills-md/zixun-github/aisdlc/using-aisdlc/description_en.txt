Use when needing to execute the AI SDLC (Spec Pack) process in the sdlc-dev repository, select/connect requirement-side (raw/solution/prd/prototype/demo) and implementation-side (plan/execute/finishing) skills, and use gates to avoid context drift, writing to the wrong directory, or skipping critical steps under pressure.
