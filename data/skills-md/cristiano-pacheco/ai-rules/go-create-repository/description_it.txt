Genera interfacce e implementazioni delle porte del repository Go seguendo le convenzioni dell'architettura modulare GO (Gorm, PingoDB, tracciamento OTEL, Fx DI, architettura delle porte). Da utilizzare durante la creazione di livelli di accesso ai dati per entità in internal/modules/<module>/ incluse operazioni CRUD (Create, FindAll, FindByID, Update, Delete), query personalizzate, impaginazione o transazioni.
