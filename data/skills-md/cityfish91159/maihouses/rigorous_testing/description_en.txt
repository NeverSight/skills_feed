Strict testing protocol. Enforce the "If it's not tested, it's not done" principle.

Trigger moments:
- When fixing a bug (must add regression test)
- When implementing new features (must have corresponding tests)
- When modifying existing code (ensure tests still pass)
- When user mentions "test", "testing", or "verify functionality"
