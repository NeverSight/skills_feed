Initialize or align the repository's documentation root directory (the docs_root name can be arbitrary): select and write to .agents/skills/capmap-system/capmap.yaml, create a documentation homepage, a thematic capability base map, specification/operations/archive indexes, a .gitignore file, and optionally Obsidian (including graph.json state color groups). This is intended for use when “initializing docs,” “organizing documentation directories,” “building docs according to the capability base map system,” or “aligning documentation structure.”
