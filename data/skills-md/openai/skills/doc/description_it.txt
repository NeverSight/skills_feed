Da utilizzare quando l'attività prevede la lettura, la creazione o la modifica di documenti ".docx", in particolare quando è importante la fedeltà della formattazione o del layout; preferisci `python-docx` più il pacchetto `scripts/render_docx.py` per i controlli visivi.
