Guida per la creazione di applicazioni con Apollo Kotlin, la libreria client GraphQL per Android e Kotlin. Utilizza questa competenza quando: (1) configuri Apollo Kotlin in un progetto Gradle per Android, Kotlin/JVM o KMP, (2) configuri il download dello schema e il codegen per i servizi GraphQL, (3) configuri un `ApolloClient` con autenticazione, intercettori e memorizzazione nella cache, (4) scrivi query, mutazioni o abbonamenti,
