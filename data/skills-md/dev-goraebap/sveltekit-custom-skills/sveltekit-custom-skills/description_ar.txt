بنية خادم مشروع SvelteKit وقواعد كتابة التعليمات البرمجية. يحدد طبقة الخادم (السجل النشط، خدمة الاستعلام، REST API)، وسياسة إعادة استخدام المكونات، وإرشادات إمكانية قراءة التعليمات البرمجية. ارجع إليها عند كتابة أو مراجعة كود SvelteKit.
