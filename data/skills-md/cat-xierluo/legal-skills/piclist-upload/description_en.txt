Use the PicList HTTP Server to upload local images in Markdown files to an image-hosting service and replace them with cloud links. This skill should be used when the user needs to upload images in Markdown, process Markdown containing local image references, batch-process multiple Markdown files or directories, or replace local paths with cloud links for cross-device access.
