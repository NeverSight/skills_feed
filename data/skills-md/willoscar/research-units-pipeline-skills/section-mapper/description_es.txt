Asigne documentos del conjunto principal a cada subsección del esquema y escriba `outline/mapping.tsv` con seguimiento de cobertura.
**Activador**: mapeador de secciones, mapeo.tsv, cobertura, mapeo de papel a sección, 论文映射, 覆盖率.
**Usar cuando**: estructura 阶段（C2），已有 `papers/core_set.csv` + `outline/outline.yml`，需要确保每小节有足够支持论文再进入 evidencia/escritura。
**Omitir si**: 还没有 esquema（先跑 `outline-builder`）或 conjunto básico 还没收敛.
**Red**: ninguna.
**Barandilla**: 覆盖率可审计（避免所有小节重复用同几篇）；为弱覆盖小节留下明确补救方向（扩consulta / 合并小节）。
