Map the papers in the core set to each subsection of the outline and write `outline/mapping.tsv` to track coverage.  
**Trigger**: section-mapper, mapping.tsv, coverage, paper-to-section mapping, paper mapping, coverage.  
**When to use**: structure stage (C2), when `papers/core_set.csv` + `outline/outline.yml` already exist, to ensure every subsection has sufficient supporting papers before moving to evidence/writing.  
**Skip if**: no outline yet (run `outline-builder` first) or core set not yet converged.  
**Network**: none.  
**Guardrails**: coverage must be auditable (avoid reusing the same batch of papers for all subsections); leave clear remediation directions for weakly-covered subsections (expand query/merge subsection).
