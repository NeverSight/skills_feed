Cartographiez les documents de l'ensemble de base dans chaque sous-section du plan et écrivez « outline/mapping.tsv » avec suivi de la couverture.
**Déclencheur** : mappeur de sections, mapping.tsv, couverture, mappage papier à section, 论文映射, 覆盖率.
**À utiliser lorsque** : structure 阶段（C2）, avec `papers/core_set.csv` + `outline/outline.yml`, avec preuve/écriture.
**Passer si** : Il s'agit de l'ensemble de base outline（先跑 `outline-builder`）或.
**Réseau** : aucun.
**Garde-corps** : 覆盖率可审计（避免所有小节重复用同几篇）；为弱覆盖小节留下明确补救方向（扩requête / 合并小节）。
