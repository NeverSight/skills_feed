Mappa i documenti dal set principale a ciascuna sottosezione dello schema e scrivi "outline/mapping.tsv" con il monitoraggio della copertura.
**Trigger**: mappatore di sezioni, mapping.tsv, copertura, mappatura da carta a sezione, 论文映射, 覆盖率.
**Utilizzare quando**: struttura 阶段（C2）,已有 `papers/core_set.csv` + `outline/outline.yml`,需要确保每小节有足够支持论文再进入 prova/scrittura.
**Salta se**: 还没有outline（先跑 `outline-builder`）或 core set 还没收敛.
**Rete**: nessuno.
**Guardrail**: 覆盖率可审计（避免所有小节重复用同几篇）；为弱覆盖小节留下明确补救方向（扩query / 合并小节）.
