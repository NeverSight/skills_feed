將論文從核心集映射到每個大綱小節，並編寫具有覆蓋範圍跟踪的“outline/mapping.tsv”。
**觸發器**：截面映射器、mapping.tsv、覆蓋率、紙張到截面映射、論文映射、覆蓋率。
**使用時**：結構階段（C2），已有`papers/core_set.csv` + `outline/outline.yml`，需要確保每小節有足夠支持論文再進入evidence/writing。
**如果**則跳過：還沒有outline（先跑`outline-builder`）或核心集尚未收斂。
**網絡**：無。
**護欄**：覆蓋率可審計（避免所有小節重複用同幾篇）；為弱覆蓋小節留下明顯的失敗方向（擴查詢/合併小節）。
