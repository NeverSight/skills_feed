使用工作区+检查点端到端运行此存储库的单元+检查点研究管道（调查/综述/评论/调研/教程/系统综述/审稿）。
**触发**：运行管道、启动、继续执行、自动运行、写文章、调查/综述/评论/调研/教程/系统综述/审稿。
**使用时机**：用户希望端到端运行流程（创建`workspaces/<name>/`、生成/执行`UNITS.csv`、遇到HUMAN检查点停止等待）。
**如果**则跳过：用户明确要手动逐条执行（用`unit-executor`），或者你不应该自动推进到散文阶段。
**网络**：取决于所选管道（arXiv/PDF/引文验证可能需要网络；支持离线导入（如果可用）。
**Guardrail**：必须尊重检查点（无批准不写散文）；遇到 HUMAN 单元必须停止等待；禁止在 repo root 创建工作空间工件。
