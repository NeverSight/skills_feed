从核心论文集和范围约束构建 2+ 级分类法 (`outline/taxonomy.yml`)，每个节点都有简短的描述。
**触发器**：分类、分类构建器、分类、主题树、taxonomy.yml。
**使用时**：survey/snapshot的结构阶段（NO PROSE），已有`papers/core_set.csv`，需要生成可映射且读者友好的主题结构。
**跳过如果**：已经批准有过且可映射的分类法（不要无意义重构）。
**网络**：无。
**护栏**：避免泛化占位桶；保持2+层且每个节点有具体描述。
