Retrieve paper metadata from arXiv via keyword queries and save results as JSONL (`papers/papers_raw.jsonl`).
**Trigger**: arXiv, arxiv, paper search, metadata retrieval, literature search, paper search, pull metadata, offline import.
**Use when**: an initial paper collection is needed (survey/snapshot Stage C1) from arXiv (online search or offline import of export).
**Skip if**: `papers/papers_raw.jsonl` already exists or data source is not arXiv.
**Network**: online search requires network; offline `--input <export.*>` does not.
**Guardrail**: metadata only; do not write long prose in `output/`.
