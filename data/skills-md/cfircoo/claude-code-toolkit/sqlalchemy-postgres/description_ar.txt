إرشادات الخبراء لـ SQLAlchemy 2.0 + Pydantic + PostgreSQL. يُستخدم عند إعداد طبقات قاعدة البيانات، أو تحديد النماذج، أو إنشاء عمليات الترحيل، أو أي عمل متعلق بقاعدة البيانات. يتم تنشيطه تلقائيًا لمهام قاعدة البيانات.
