Use @aptx/api-plugin-retry to implement request retries. It configures retry attempts, sets fixed or dynamic delay strategies, defines retryOn conditions (NetworkError/TimeoutError), prevents retrying non-idempotent requests, tracks retry counts, and provides the createRetryMiddleware API. Trigger it when your code needs retry functionality or createRetryMiddleware.
