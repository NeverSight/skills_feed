Use @aptx/token-store to define or implement token persistence. Used for: implementing the TokenStore interface, supporting sync/async APIs (getToken/setToken/clearToken), optional metadata methods, implementing different storage backends (cookie, localStorage, mini-program, memory), and working with @aptx/api-plugin-auth. Triggered when code needs to implement a custom TokenStore.
