Guide the LLM to correctly use the React Query and Vue Query hooks generated by the aptx code generator. Trigger this when the LLM modifies parameters (such as adding enabled, staleTime, etc.) while using the generated useXxxQuery or useXxxMutation hooks in the project.
