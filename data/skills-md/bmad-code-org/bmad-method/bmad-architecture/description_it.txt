Elabora e registra le decisioni sull'architettura che mantengono coerenti le parti di un sistema costruite separatamente, in un breve documento di architettura. Crea, aggiorna o convalida uno; funziona da una specifica, da un'idea grezza o da una base di codice esistente. Da utilizzare quando l'utente dice "crea l'architettura", "crea l'architettura tecnica", "la colonna vertebrale dell'architettura" o "crea il progetto di una soluzione"
