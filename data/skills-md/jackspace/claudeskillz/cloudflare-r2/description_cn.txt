Cloudflare R2 的完整知识领域 - Cloudflare 边缘网络上与 S3 兼容的对象存储。

使用场合：创建 R2 存储桶、将文件上传到 R2、下载对象、配置 R2 绑定、
设置 CORS、生成预签名 URL、分段上传、存储图像/资产、管理对象
元数据，或遇到“R2_ERROR”、CORS 错误、预签名 URL 失败、分段上传问题、
或存储配额错误。

关键词：r2、r2 存储、cloudflare r2、r2 存储桶、r2 上传、r2 下载、r2 绑定、对象存储、
s3 兼容、r2 cors、预签名 url、分段上传、r2 api、r2 工作人员、文件上传、资产存储、
R2_ERROR、R2Bucket、r2 元数据、自定义元数据、http 元数据、内容类型、缓存控制、
aws4fetch、s3 客户端、批量删除、r2 列表、存储类
