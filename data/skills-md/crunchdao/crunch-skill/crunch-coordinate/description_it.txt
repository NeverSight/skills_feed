Da utilizzare quando si gestiscono coordinatori Crunch, competizioni (crunch), premi, checkpoint, staking o conti cruncher tramite crunch-cli. Da utilizzare anche quando si lavora all'interno di un'area di lavoro del nodo coordinatore (contiene la cartella .agent/, docker-compose.yml, runtime_definitions/ o CrunchConfig).
