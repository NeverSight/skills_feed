Angular v20+ でシグナルベースのリアクティブ状態管理を実装します。 signal() でリアクティブ状態、computed() で派生状態、linkedSignal() で依存状態、effect() で副作用を作成するために使用します。状態管理の質問をトリガーし、BehaviorSubject/Observable パターンからシグナルへの変換、またはリアクティブなデータ フローの実装を行います。
