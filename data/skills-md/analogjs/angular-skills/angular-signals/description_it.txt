Implementa la gestione dello stato reattivo basata sul segnale in Angular v20+. Utilizzare per creare uno stato reattivo con signal(), uno stato derivato con computed(), uno stato dipendente con linkedSignal() ed effetti collaterali con effect(). Trigger su domande di gestione dello stato, conversione da modelli BehaviorSubject/Observable in segnali o implementazione di flussi di dati reattivi.
