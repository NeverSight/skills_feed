When the user mentions ARCS/arcs-sdk, cross-compilation toolchain (riscv64-unknown-elf-gcc), cskburn, flashing, /dev/ttyACM*, serial logs, etc., use it for: pulling repositories, environment setup, compilation, flashing, running, and log reading; it does not handle code writing/understanding—code development is handled by Claude Code itself.
