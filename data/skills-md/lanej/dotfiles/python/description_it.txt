Utilizza uv per la gestione rapida dei progetti Python, l'esecuzione di script, la gestione delle dipendenze e l'installazione degli strumenti. EVITARE pip: utilizza sempre i comandi uv (uv add, uv sync, uv run).
