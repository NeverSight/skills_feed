بناء صور حاوية ذات مستوى عالمي - ملفات Dockerfiles، والإنشاءات متعددة المراحل، وتقوية الأمان، وندوب المعركة من الصور التي اندلعت في الإنتاج، استخدم عندما "docker، dockerfile، Container، image، docker-compose، build، multi-stage، alpine، distroless، الصفر، docker build، docker run،registry، ecr، gcr، dockerhub، Layer، ذاكرة التخزين المؤقت، docker، حاويات، ملف dockerfile، الصور، النقل بالحاويات، devops، الخدمات المصغرة السحابية الأصلية" المذكورة.
