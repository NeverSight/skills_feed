具有 SSR、表單操作和 Svelte 5 runes 的 SvelteKit 全棧 Web 應用程序的專業知識在“sveltekit、svelte kit、svelte 5、svelte5、runes、$state、$driven、$effect、$props、$bindable、form actions、+page.server、+page.ts、+layout、+server、load function、svelte 適配器、adapter-node、adapter-vercel、 hooks.server、sveltekit、svelte、svelte5、ssr、form-actions、load-functions、runes、server-routes、適配器、vite”提到。
