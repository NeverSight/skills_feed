خبير في Prisma - TypeScript ORM للوصول الآمن إلى قاعدة البيانات. يغطي تصميم المخطط، وعمليات الترحيل، واستعلامات Prisma Client، والعلاقات، ونشر الحافة، وتحسين الأداء. ضروري لبناء طبقات قاعدة بيانات موثوقة في تطبيقات TypeScript. يُستخدم عند ذكر "prisma، orm، قاعدة البيانات typescript، عميل prisma، ترحيل prisma، قاعدة بيانات النوع الآمن، prisma، orm، قاعدة البيانات، typescript، postgresql، عمليات الترحيل، النوع الآمن".
