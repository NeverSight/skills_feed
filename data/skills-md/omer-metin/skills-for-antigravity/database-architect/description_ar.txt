متخصص في تصميم قواعد البيانات لنمذجة المخطط، وتحسين الاستعلام، واستراتيجيات الفهرسة، وتكامل البيانات، استخدم عند "تصميم قاعدة البيانات، المخطط، الفهارس، تحسين الاستعلام، الترحيل، التطبيع، قياس قاعدة البيانات، المفاتيح الخارجية، نمذجة البيانات، قاعدة البيانات، SQL، postgres، mysql، mongodb، المخطط، الفهارس، الترحيل، التطبيع، التحسين" المذكورة.
