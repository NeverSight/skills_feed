Tencent Map location service, supporting POI search, route planning, travel planning, nearby search, trajectory data visualization, and map data visualization. ⚠️ Mandatory behavior: After this skill loads, the first action must be to check whether an official key exists (environment variable TMAP_WEBSERVICE_KEY or the user has already provided one in the conversation). If an official key is already present, proceed directly to handle the user’s request. If no official key is present, you must immediately present the user with a key selection prompt (official key registration guide + preset trial key option), then pause awaiting the user’s selection, and you may not perform any analysis, read files, plan solutions, or write code before the user makes a selection.
