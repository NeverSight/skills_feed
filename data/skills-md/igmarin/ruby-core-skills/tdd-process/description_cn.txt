使用硬门强制执行红绿重构：红色阶段写入必须在断言（而不是语法/配置）上失败的失败测试，​​并在继续之前显示测试+失败，在每个阶段后对特定文件（不是完整套件）运行测试，绿色阶段写入最少的代码以通过并在那里停止，重构阶段在每次微更改后运行测试，并且必须始终保持绿色。生成失败的测试用例，在继续之前验证测试失败，在通过红阶段检查后控制实施。当用户请求 TDD 工作流程、要求首先编写测试、提及红绿重构方法或使用触发词：TDD、测试优先、测试门时使用。
