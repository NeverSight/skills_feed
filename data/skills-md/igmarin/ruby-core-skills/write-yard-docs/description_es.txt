Úselo al escribir documentación YARD para API públicas de Ruby: cada método público DEBE tener etiquetas `@param`, `@return [Hash]` y `@raise`, documentar `self.call` por separado de `#call`, enumerar cada excepción con su propia etiqueta `@raise`, usar `@example` para construcciones a nivel de módulo, `@see` para referencias cruzadas, seguir las convenciones de anotación de tipo YARD `@return`, agregar subtareas explícitas de YARD después implementación en listas de tareas, mantenga todo el texto de YARD en inglés a menos que se solicite lo contrario, ejecute `yard stats --list-undoc` para verificar la cobertura y cargue archivos de recursos extendidos solo cuando se necesite su contenido específico. Palabras desencadenantes: YARD, documentos en línea, documentación de métodos, documentos API, interfaz pública, rdoc, etiqueta de retorno, etiqueta de aumento.
