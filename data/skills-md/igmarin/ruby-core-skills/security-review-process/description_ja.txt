OWASP Top 10 にマッピングされた Ruby コードのセキュリティ レビュー手順を標準化します。処理前にすべての入力パラメータを許可リストに登録し、SQL 補間を禁止し (`#{}`)、コミットまたはログに記録されたシークレットがないことを確認し、`bundle execbundle-audit check --update` を実行し、シェル インジェクション (`system()`、バックティック、`exec()`) をチェックし、命令のようなキー (`prompt`、`instructions`) を破棄します。 JSON ペイロード。入力検証、シークレット管理、依存関係の監査について説明します。トリガーワード: セキュリティレビュー、セキュリティチェック、監査コード、セキュリティ脆弱性、機密チェック、OWASP。
