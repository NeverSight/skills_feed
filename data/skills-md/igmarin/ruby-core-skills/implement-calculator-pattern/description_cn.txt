通过 `Factory.for(entity)` 构建带有 SERVICE_MAP 路由的基于变体的计算器时使用（仅允许的入口点 - 禁止直接实例化），BaseService 如果 `should_calculate?` 定义 `calculate`→`compute_result`，NullService 有 `should_calculate?`=false+`compute_result`=nil，具体服务覆盖这两个方法并且必须调用 `super` `should_calculate?` — 每个组件都按顺序进行测试（在下一个组件之前编写规范 → 运行 → 验证红色 → 实现 → 运行 → 验证绿色，而不会将 NullService 和具体内容分解为一个步骤）。测试覆盖范围包括：命名变体、非活动计划、零计划和未知变体上下文。每个变体的支架测试。触发词：策略模式、工厂模式、空对象模式、变量计算器、调度表、SERVICE_MAP、无操作回退。
