Wird beim Erstellen variantenbasierter Rechner mit SERVICE_MAP-Routing über „Factory.for(entity)“ verwendet (NUR erlaubter Einstiegspunkt – direkte Instanziierung VERBOTEN), BaseService definiert „calculate“ → „compute_result“, wenn „should_calculate?“, NullService hat „should_calculate?“=false+`compute_result`=nil, konkrete Dienste überschreiben beide Methoden und MÜSSEN „super“ aufrufen in „should_calculate?“ – und jede Komponente wird der Reihe nach getestet (Spezifikation schreiben → ausführen → Rot überprüfen → implementieren → ausführen → Grün vor der nächsten Komponente überprüfen, ohne NullService und Beton in einem Schritt zusammenzufassen). Die Testabdeckung umfasst: benannte Varianten, inaktiver Plan, Nullplan und unbekannte Variantenkontexte. Gerüsttests pro Variante. Auslösewörter: Strategiemuster, Fabrikmuster, Nullobjektmuster, Variantenrechner, Versandtabelle, SERVICE_MAP, No-Op-Fallback.
