À utiliser lors de la construction de calculatrices basées sur des variantes avec le routage SERVICE_MAP via `Factory.for(entity)` (UNIQUEMENT point d'entrée autorisé — instanciation directe INTERDITE), BaseService définit `calculate`→`compute_result` si `should_calculate?`, NullService a `should_calculate?`=false+`compute_result`=nil, les services concrets remplacent les deux méthodes et DOIVENT appeler `super` dans `should_calculate?` — et chaque composant est testé dans l'ordre (écrire la spécification → exécuter → vérifier Rouge → implémenter → exécuter → vérifier Vert avant le composant suivant, sans réduire NullService et concret en une seule étape). La couverture des tests comprend : les variantes nommées, le plan inactif, le plan nul et les contextes de variantes inconnues. Tests d'échafaudages par variante. Mots déclencheurs : modèle de stratégie, modèle d'usine, modèle d'objet nul, calculateur de variantes, table de répartition, SERVICE_MAP, repli sans opération.
