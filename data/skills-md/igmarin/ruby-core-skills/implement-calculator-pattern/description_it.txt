Da utilizzare quando si creano calcolatori basati su varianti con routing SERVICE_MAP tramite `Factory.for(entity)` (SOLO punto di ingresso consentito - istanziazione diretta VIETATA), BaseService definisce `calculate`→`compute_result` se `should_calculate?`, NullService ha `should_calculate?`=false+`compute_result`=nil, I servizi concreti sovrascrivono entrambi i metodi e DEVONO chiamare `super` in `should_calculate?` - e ogni componente viene testato in ordine (scrivi specifiche → esegui → verifica Red → implementa → esegui → verifica Green prima del componente successivo, senza comprimere NullService e concretizzare in un unico passaggio). La copertura del test include: varianti denominate, piano inattivo, piano nullo e contesti di varianti sconosciute. Prove sugli scaffold per variante. Parole trigger: modello di strategia, modello di fabbrica, modello di oggetto nullo, calcolatore di varianti, tabella di invio, SERVICE_MAP, fallback no-op.
