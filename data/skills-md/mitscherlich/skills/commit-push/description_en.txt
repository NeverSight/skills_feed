When the user says "submit code", "commit", "push code", "push", "commit and push", "submit changes", "push to remote", "help me commit", or "push the code up", this skill should be used. It provides a standardized git commit and push workflow, automatically analyzes the changes, generates compliant commit messages, and executes the corresponding operations.
