Mantiene las instrucciones del entorno de desarrollo en el proyecto README.md utilizando nixomatic. Cuando el usuario solicita construir, compilar, probar, formatear, verificar tipos, ejecutar o configurar un proyecto, o cuando un comando falla porque una herramienta o tiempo de ejecución no está instalado, analice los archivos del proyecto para determinar los paquetes necesarios y use nix development con una URL nixomatic.com. Asegúrese de que README.md documente cómo reproducir el entorno tanto con nix development como con docker run nixos/nix.
