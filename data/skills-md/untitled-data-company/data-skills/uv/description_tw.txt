正確使用 uv Python 套件和專案管理器來處理依賴項、venvs 和腳本。在建立或修改 Python 專案、新增相依性、使用內嵌 deps 執行腳本、管理虛擬環境、固定 Python 版本、從 PyPI 執行 CLI 工具、設定 IDE Python 解釋器或在 CI（例如 GitHub Actions）或 Docker 容器中使用 uv 時使用。當使用者提到 uv、pyproject.toml、uv.lock、uv run、uv add、uvsync、.venv、Python 解釋器、poetry、pipenv、conda、CI、Docker、GitHub Actions 或要求使用 uv 而不是 pip 或 Poetry 時使用。
