正确使用 uv Python 包和项目管理器来处理依赖项、venvs 和脚本。在创建或修改 Python 项目、添加依赖项、使用内联 deps 运行脚本、管理虚拟环境、固定 Python 版本、从 PyPI 运行 CLI 工具、设置 IDE Python 解释器或在 CI（例如 GitHub Actions）或 Docker 容器中使用 uv 时使用。当用户提到 uv、pyproject.toml、uv.lock、uv run、uv add、uvsync、.venv、Python 解释器、poetry、pipenv、conda、CI、Docker、GitHub Actions 或要求使用 uv 而不是 pip 或 Poetry 时使用。
