Automatically manages blogs, books, and pages content for GitSite static-site-generator projects. Use this skill when users need to create blog posts, book chapters, books, or static pages. It automatically builds the correct directory structure, generates Markdown template files with complete frontmatter, and provides Python scripts for content management. Suitable for static websites built with the GitSite CLI.
