Use when the user explicitly requests "generate NSFC schematic/mechanism diagram" or needs to convert the research mechanism, algorithm architecture, or module relationships in a proposal into an "editable + embeddable" illustration. Deliver editable source file (`.drawio`) and deliverable render file (`.svg`/`.png`, optionally `.pdf`), with support for multi-round evaluation and refinement. ⚠️ Not applicable: the user only wants to polish body text (should directly rewrite text), only wants to change format/size of an existing image (should use image processing skill), or has not clearly expressed intent for "schematic/mechanism diagram."
