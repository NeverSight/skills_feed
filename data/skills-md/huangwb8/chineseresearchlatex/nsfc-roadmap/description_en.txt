Use only when the user explicitly asks to “generate an NSFC technology roadmap / roadmap drawing / roadmap / flowchart” or needs to convert proposal research content into a “printable, A4-readable” technology roadmap. Output editable source files (`.drawio`) and renderings embeddable in documents (`.svg`/`.png`/`.pdf`). ⚠️ Not applicable: the user merely wants to change the format/size of an existing image (use image-processing skill) or simply polish the textual description of the technology roadmap (rewrite the main text directly).
