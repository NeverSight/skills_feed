Git Operations Safety and Standards. Requires the use of native Git commands for versioned files to prevent index loss or redundant changes. Triggered when an Agent attempts to move, rename, or delete files.
