Validez les modifications de code en sélectionnant et en exécutant intelligemment les suites de tests appropriées. Utilisez-le lors de la modification du code pour vérifier que les modifications fonctionnent correctement, exécuter des tests, valider les fonctionnalités ou vérifier les régressions. Détecte automatiquement les suites de tests concernées, sélectionne l'ensemble minimal de venvs nécessaire à la validation et gère l'exécution des tests avec les services Docker si nécessaire.
