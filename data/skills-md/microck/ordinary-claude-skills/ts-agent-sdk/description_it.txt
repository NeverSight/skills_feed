Genera SDK TypeScript tipizzati per consentire agli agenti AI di interagire con i server MCP. Converte comandi curl JSON-RPC dettagliati in chiamate di funzione pulite (docs.createDocument() vs curl). Rileva automaticamente gli strumenti MCP dai moduli server, genera tipi TypeScript e metodi client, crea script di esempio eseguibili.

Da utilizzare quando: si creano applicazioni abilitate per MCP, è necessario l'accesso programmatico digitato agli strumenti MCP, si desidera che Claude Code gestisca le app tramite script, si eliminino i comandi curl JSON-RPC manuali, si convalidino input/output MCP o si crei un'automazione dell'agente riutilizzabile.
