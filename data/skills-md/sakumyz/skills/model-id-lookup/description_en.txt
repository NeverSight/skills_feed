Skill to query AI model IDs. Use this skill when users need to find or verify AI model IDs. It supports checking local cache first; if not found, it updates local data from online models.dev and then queries.
