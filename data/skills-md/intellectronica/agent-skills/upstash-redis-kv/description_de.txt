Lesen und Schreiben in den Upstash Redis-kompatiblen Schlüsselwertspeicher über die REST-API. Verwenden Sie diese Option, wenn Schlüsselwertdaten gespeichert oder abgerufen werden müssen, Redis-Funktionen (Caching, Zähler, Listen, Sätze, Hashes, sortierte Sätze usw.) für die aktuelle Interaktion verwendet werden müssen oder wenn der Benutzer ausdrücklich die Verwendung von Upstash oder Redis anfordert.
