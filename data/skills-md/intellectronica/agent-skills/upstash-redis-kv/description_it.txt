Leggi e scrivi nell'archivio chiave-valore compatibile con Upstash Redis tramite API REST. Da utilizzare quando è necessario salvare o recuperare dati di valori-chiave, utilizzare le funzionalità Redis (caching, contatori, elenchi, set, hash, set ordinati, ecc.) per l'interazione corrente o quando l'utente richiede esplicitamente di utilizzare Upstash o Redis.
