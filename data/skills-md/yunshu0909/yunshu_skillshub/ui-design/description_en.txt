UI style modification collaboration workflow. Used when users request changes to page styling, layout adjustments, or UI detail tweaks. Through a structured process of "screenshot positioning → current state description → solution selection → code modification → fine-tuning," it reduces communication deviation and avoids token waste.
