Generate or update the PROJECT_MAP.md for a user-specified folder. Suitable when the user requests a directory map/project map/repository overview/folder-level description/update of an existing PROJECT_MAP.md. You must first ask which folder(s) to scan; full-repository scanning is prohibited by default; supports single or multiple directories (merged or separate generation).
