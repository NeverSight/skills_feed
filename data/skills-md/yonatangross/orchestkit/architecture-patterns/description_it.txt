Convalida dell'architettura e modelli per un'architettura pulita, applicazione della struttura backend, convalida della struttura del progetto, standard di test e dimensionamento sensibile al contesto. Da utilizzare durante la progettazione dei confini del sistema, l'applicazione di un'architettura a più livelli, la convalida della struttura del progetto, la definizione di standard di test o la scelta del livello di architettura corretto per l'ambito del progetto.
