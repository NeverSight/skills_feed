當專案依賴於`@ nomicfoundation/hardhat-toolbox-viem'時，與`hardhat`技能一起使用。涵蓋'network.create ()`、合約互動(`viem.deployContract'、`read`、`write`、`getContractAt`)和`viem.assertions` (revert/event/balance assertions)上公開的viem用戶端。
