Generate/maintain BDD/ezSpec tests from the acceptance.yaml in the spec catalog. Using Gherkin-like syntax, AI automatically generates step definitions (developers don't need to hand-write them), so the acceptance test specification is an Executable Specification.
