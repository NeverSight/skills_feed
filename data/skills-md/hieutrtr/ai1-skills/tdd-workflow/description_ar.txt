تطبيق سير عمل التطوير القائم على الاختبار لمشاريع Python وReact. يُستخدم عندما يطلب المستخدم TDD، أو تطوير الاختبار الأول، أو منهجية إعادة البناء باللونين الأحمر والأخضر. يفرض دورة صارمة: اكتب اختبارًا فاشلًا واحدًا -> قم بتنفيذ الحد الأدنى من التعليمات البرمجية للتمرير -> إعادة البناء باللون الأخضر -> كرر. ينطبق على كل من الواجهة الخلفية (pytest) والواجهة الأمامية (مكتبة الاختبار). يغير سلوك الوكيل لكتابة الاختبارات قبل التعليمات البرمجية. لا يوفر أنماط الاختبار (استخدم أنماط pytest أو أنماط اختبار التفاعل لكيفية كتابة الاختبارات).
