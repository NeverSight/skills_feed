Meccanica del framework FastAPI e pattern avanzati. Da utilizzare durante la configurazione del middleware, la creazione di catene di inserimento delle dipendenze, l'implementazione di endpoint WebSocket, la personalizzazione della documentazione OpenAPI, l'impostazione di CORS, la creazione di dipendenze di autenticazione (convalida JWT, accesso basato sui ruoli), l'implementazione di attività in background o la gestione della durata dell'applicazione (avvio/arresto). NON copre il CRUD dell'endpoint di base o i modelli di repository/servizio (usare python-backend-expert) o i test (usare pytest-patterns).
