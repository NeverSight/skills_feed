Utilizzare per le operazioni CLI Base44 e l'inizializzazione del progetto. Trigger: l'utente vuole creare/inizializzare un nuovo progetto Base44; l'utente menziona i comandi CLI (npx base44, Yarn base44, create, login, logout, whoami, deploy, entità push, site deploy, funzioni deploy); la directory è vuota o manca base44/config.jsonc; l'utente dice "crea un'app/progetto Base44", "configura Base44", "distribuisci su Base44", "entità push". Questa abilità è il PREREQUISITO per base44-sdk: utilizzala sempre per i nuovi progetti prima di creare funzionalità.
