検証付きの凍結されたデータクラス パターンを使用して、不変のドメイン値オブジェクトを作成します。
ドメイン値オブジェクトを実装する場合、不変のデータ構造を作成する場合、または
値に検証を追加します。 @dataclass(frozen=True)、object.__setattr__() をカバーします。
__post_init__ のパターン、ファクトリ メソッド (from_string、from_dict、from_content)、
凍結されたコンテキストでの検証。 「X の値オブジェクトの作成」、「実装」でトリガー
不変の Y 値」、「Z 値に検証を追加」、または「値オブジェクトを構築」。
