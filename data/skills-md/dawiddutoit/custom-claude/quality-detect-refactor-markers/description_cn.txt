查找代码库中的所有 REFACTOR 标记，验证关联的 ADR 是否存在，识别过时的标记
（30 天以上），并检测孤立标记（无 ADR 参考）。在状态检查期间使用，之前
功能完成，或用于重构健康审核。触发“检查重构状态”、“标记
health”、“状态是什么”，或者在将功能标记为完成之前主动进行。与
Python (.py)、TypeScript (.ts) 和 JavaScript (.js) 文件使用 grep 模式来定位标记
并根据 docs/adr/ 目录中的 ADR 文件进行验证。
