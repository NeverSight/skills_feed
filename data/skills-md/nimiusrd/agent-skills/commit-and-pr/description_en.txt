Commit the changes and create a GitHub Pull Request. Used for requests like “commit and open a PR,” “commit my current work and create a pull request,” or “push the changes and open a PR.” Handles staging, generating a commit message, creating a branch, pushing, and opening the PR as a single workflow. The commit message and PR body are written in Japanese.
