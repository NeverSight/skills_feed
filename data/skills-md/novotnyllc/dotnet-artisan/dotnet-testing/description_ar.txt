يحدد استراتيجية اختبار .NET وأنماط التنفيذ عبر xUnit v3 (الحقائق، والنظريات، والتركيبات، وIAsyncLifetime)، واختبار التكامل (WebApplicationFactory، وTestcontainers)، واختبار Aspire (DistributedApplicationTestingBuilder)، واختبار اللقطة (التحقق، والتنقية)، وأتمتة متصفح Playwright E2E، ومقاييس BenchmarkDotNet الدقيقة، وتغطية الكود (Coverlet)، واختبار الطفرات. (Stryker.NET)، واختبار واجهة المستخدم (كائنات الصفحة، والمحددات)، وتجميع اختبار AOT WASM. يمتد إلى 13 مجالًا للموضوع. لا تستخدمه في بنية واجهة برمجة التطبيقات (API) للإنتاج أو تأليف سير عمل CI.
