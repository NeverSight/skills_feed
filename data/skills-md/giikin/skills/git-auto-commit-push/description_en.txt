Git automatically stages, commits, and pushes. Use this skill when the user asks to commit code, push code, commit, push, commit and push, auto-commit, stage-commit-push, or any operation involving git commit/push. Also applies when the user says "commit it", "push the code", "help me commit", "push it up", "commit only", "just commit", or similar colloquial expressions.
