在“flow_definition.py”（流程包）中创建、调试和优化基于 flow.kit 的 test.guide 工作流程。使用 `FlowBuilder`/`add_block_with`/`Assign` 设计或修改流程、选择/连接 flow.kit 块（触发器、test.guide、控制流、用户代码）、运行 `main.py` 来验证/执行/可视化，或查阅 `.vscode/flow-kit-snippets.code-snippets` 和 `flow_kit/**/*.pyi` / `site-packages/**/*.pyi` 了解 API 详细信息。
