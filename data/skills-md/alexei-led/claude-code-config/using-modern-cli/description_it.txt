Preferisci i moderni strumenti CLI per prestazioni migliori: rg (ripgrep) invece di grep per la ricerca di testo, fd invece di find per l'individuazione di file, bat invece di cat per visualizzare file, sd invece di sed per la sostituzione del testo, eza invece di ls per l'elenco delle directory, polvere invece di du, procs invece di ps. Da utilizzare quando si scrivono script bash, si ottimizzano le catene di comandi, si lavora con le ricerche di file o si sostituisce grep/find/cat nei flussi di lavoro. Questi strumenti sono più veloci, rispettano .gitignore e hanno una migliore formattazione dell'output.
