Public-opinion monitoring skill; final deliverable: a public-opinion report. Use this skill when the user asks “What’s the public opinion on X/incident?” “How’s the sentiment?” “Run sentiment analysis” or requests crawling by keyword/platform and generating a report. Agreement: any intent related to public opinion triggers the full workflow (crawl → sentiment analysis → report). Crawling is handled by this skill; sentiment analysis is performed autonomously by the main Agent via prompts; the report is generated by generate_report.
