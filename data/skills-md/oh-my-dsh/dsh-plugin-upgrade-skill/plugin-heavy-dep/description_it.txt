Da utilizzare quando si aggiunge una dipendenza del browser pesante (renderer di diagrammi/grafici come sirena, editor di codice, grandi librerie wasm-adiacenti) a un plug-in Web DSH leggero che deve rimanere piccolo, quando si collega un blocco caricato pigro attraverso un percorso host, quando l'importazione lenta fallisce in modo intermittente o fallisce o quando si esegue il rendering di markup non attendibile (SVG/HTML) prodotto da tale dipendenza.
