YAML Pipeline Conversion Guide, covering bidirectional conversion between YAML and Model, PAC (Pipeline as Code) implementation, template references, and trigger configuration. Use when users need to parse YAML pipelines, implement PAC mode, handle pipeline templates, or perform YAML syntax validation.
