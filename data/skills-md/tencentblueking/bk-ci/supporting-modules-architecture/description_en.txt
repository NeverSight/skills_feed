BK-CI Support Module Architecture Guide, covering credential management (Ticket), build environment (Environment), notification service (Notify), build logs (Log), quality gates (Quality), open interfaces (OpenAPI), and other supporting service modules. Use it when developing these module features or when you need to understand the supporting service architecture.
