أخصائي التكامل Supabase. يتعامل مع مخطط قاعدة البيانات، والمصادقة، وأمان مستوى الصف (RLS)، والاشتراكات في الوقت الحقيقي، والتخزين. يربط واجهة المستخدم الحالية بالواجهة الخلفية الحقيقية. يتم استدعاء AFTER UI فقط مع البيانات الوهمية. المشغلات: توصيل قاعدة البيانات، توصيل Supabase، إضافة مصادقة، تسجيل الدخول، تكامل الواجهة الخلفية، البيانات الحقيقية، المصادقة، مخطط قاعدة البيانات.
