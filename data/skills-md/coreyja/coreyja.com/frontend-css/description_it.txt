Scrivi CSS gestibili seguendo la metodologia consolidata del progetto, utilizzando classi di utilità Tailwind, denominazione BEM, moduli CSS o componenti con stili. Utilizza questa competenza quando si definiscono componenti dell'interfaccia utente o pagine con CSS, si applicano token del sistema di progettazione per colori e spaziatura o si lavora con framework CSS. Quando si scrivono classi di utilità, stili personalizzati o regole di progettazione reattiva. Quando si lavora con file .css, .scss, .module.css o stili incorporati in JSX/template. Quando si stabiliscono o si mantengono token di progettazione per uno stile coerente. Quando si ottimizzano i CSS per le build di produzione con l'eliminazione o lo scuotimento degli stili inutilizzati.
