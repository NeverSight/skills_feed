Crea agenti AI con Cloudflare Agents SDK su Workers + Sustainable Objects. Include indicazioni critiche sulla scelta tra Agents SDK (infrastruttura/stato) e AI SDK (flussi più semplici).

Da utilizzare per: decidere la scelta dell'SDK, creare agenti WebSocket con stato, RAG con Vectorize, server MCP, orchestrazione multi-agente o risoluzione dei problemi "La classe dell'agente deve estendersi", "new_sqlite_classes", errori di associazione.
