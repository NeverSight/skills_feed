Crea con le API stateless di OpenAI: completamenti chat (GPT-5, GPT-4o), incorporamenti, immagini (DALL-E 3), audio (Whisper + TTS) e moderazione. Include l'SDK Node.js e approcci basati sul recupero per i Cloudflare Worker.

Utilizzare quando: implementare completamenti chat con GPT-5/GPT-4o, trasmettere risposte in streaming con SSE, utilizzare chiamate/strumenti di funzioni, creare output strutturati con schemi JSON, generare incorporamenti per RAG (text-embedding-3-small/large), generare immagini con DALL-E 3, modificare immagini con GPT-Image-1, trascrivere audio con Whisper, sintetizzare parlato con TTS (11 voci), moderare contenuti (11 sicurezze categorie) o limiti di velocità per la risoluzione dei problemi (429), chiavi API non valide (401), errori di chiamata di funzioni, errori di analisi dello streaming, mancate corrispondenze delle dimensioni di incorporamento o limite di token superato.
