使用 @modelcontextprotocol/sdk 在 Cloudflare Workers 上使用 TypeScript 构建无状态 MCP 服务器。提供工具、资源、提示和身份验证（API 密钥、OAuth、零信任）的模式。

在向 LLM 公开 API、集成 Cloudflare 服务（D1、KV、R2、Vectorize）或排除导出语法错误、未封闭的传输泄漏或 CORS 错误配置时使用。
