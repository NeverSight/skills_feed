使用 @modelcontextprotocol/sdk 在 Cloudflare Workers 上使用 TypeScript 構建無狀態 MCP 服務器。提供工具、資源、提示和身份驗證（API 密鑰、OAuth、零信任）的模式。

在向 LLM 公開 API、集成 Cloudflare 服務（D1、KV、R2、Vectorize）或排除導出語法錯誤、未封閉的傳輸洩漏或 CORS 錯誤配置時使用。
