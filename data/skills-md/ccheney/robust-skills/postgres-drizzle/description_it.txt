Best practice per PostgreSQL e Drizzle ORM. Trigger su: PostgreSQL, Postgres, Drizzle, database,
schema, tabelle, colonne, indici, query, migrazioni, ORM, relazioni, join, transazioni, SQL,
kit drizzle, pooling delle connessioni, N+1, JSONB, RLS.

Da utilizzare quando: si scrivono schemi di database, query, migrazioni o qualsiasi codice relativo al database.
Applica in modo proattivo durante la creazione di API, backend o modelli di dati.
