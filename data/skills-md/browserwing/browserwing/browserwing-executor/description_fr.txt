Contrôlez l'automatisation du navigateur via l'API HTTP. Prend en charge la navigation dans les pages, l'interaction des éléments (clic, saisie, sélection), l'extraction de données, l'analyse des instantanés d'accessibilité, la capture d'écran, l'exécution de JavaScript et les opérations par lots.
