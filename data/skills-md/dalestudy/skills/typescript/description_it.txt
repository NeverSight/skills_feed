Definizioni di tipo TypeScript e competenze relative alle best practice. Utilizzare nelle seguenti situazioni: (1) quando si scrivono o si modificano file TypeScript (.ts, .tsx), (2) quando si lavora sulle definizioni di tipo (interfaccia, tipo), (3) quando si regolano le impostazioni tsconfig.json o le opzioni del compilatore, (4) quando si risolvono errori di tipo o si migliora la sicurezza dei tipi, (5) quando si lavora con generici, tipi di utilità o manipolazione di tipi, (6) quando si gestiscono attività che coinvolgono le parole chiave "typescript", "ts", "type", 'interfaccia', 'generico'
