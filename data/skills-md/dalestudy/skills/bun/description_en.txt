Skills for using Bun runtime instead of Node.js. Use in the following situations: (1) when creating a new JavaScript/TypeScript project, (2) when working with package.json or dependencies, (3) when running scripts or starting a development server, (4) when migrating a Node.js project to Bun, (5) when working with tasks involving the keywords 'bun', 'bunx', 'bun.lockb'
