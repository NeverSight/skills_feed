Piracy Detection and Post-Processing Orchestrator. Used when the wechat-search-collector comprehensive-page task is complete; it performs piracy detection on capture_results, creates subtasks, writes webhook schedules, or executes webhook dispatch/reconcile after subtasks reach their final state. Supports either SQLite or Supabase as the capture_results data source.
