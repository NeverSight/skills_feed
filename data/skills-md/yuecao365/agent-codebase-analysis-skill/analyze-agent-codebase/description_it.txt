Utilizzare durante l'analisi di un repository di agenti LLM, cablaggio di agenti, runtime ad agente singolo, sistema multi-agente, framework di agenti o progetto di agente open source maturo per comprendere o presentare la progettazione del sistema, il comportamento di runtime, la gestione del contesto/stato, gli strumenti, le autorizzazioni, la memoria, il RAG, gli eventi, gli artefatti, l'osservabilità, le valutazioni, il modello di coordinamento, i diagrammi dell'architettura della sirena o il report sull'architettura HTML in linguaggio semplice.
