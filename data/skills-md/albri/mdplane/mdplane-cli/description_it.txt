Utilizza la CLI mdplane per le operazioni dello spazio di lavoro da una shell (init, read/cat, append, write, search, export, login,claim). Si attiva quando gli utenti richiedono l'utilizzo del comando mdplane o l'automazione della riga di comando. Per l'utilizzo HTTP-first, utilizzare mdplane.
