When the user wants to do Spec-driven development, you MUST use this Skill: first converge the requirements and constraints into an implementable and reviewable Spec, then proceed to implementation. This Skill produces only one Spec file written to /docs/specs/ and makes one git commit (committing only that Spec). This Skill writes no implementation code.
