Synchronize the incremental specification in the change to the master specification. Used when the user wants to update the master specification with changes from the incremental specification without archiving the change.
