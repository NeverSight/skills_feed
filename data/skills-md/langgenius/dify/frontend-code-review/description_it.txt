Si attiva quando l'utente richiede una revisione dei file frontend (ad esempio, `.tsx`, `.ts`, `.js`). Supporta sia le revisioni di modifiche in sospeso che le revisioni mirate di file durante l'applicazione delle regole della lista di controllo.
