Generate a reference template with a complete property-by-property explanation for a single Terraform AWS resource. When the user supplies the resource name (e.g., aws_s3_bucket, aws_lambda_function), output a template that contains an exact attribute list based on the Provider schema plus explanations drawn from the official AWS documentation. Use this for requests such as “Create a template for aws_xxx” or “Show me every property of aws_xxx.”
