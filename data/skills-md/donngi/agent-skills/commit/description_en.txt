Create commits following commit conventions (automatically categorizing and splitting changes). Used when the user says “commit,” “commit changes,” “/commit,” etc. Follows Conventional Commits-based conventions, automatically generating appropriate type/scope/subject. If changes should be split into multiple logical units, it automatically splits them into several commits.
