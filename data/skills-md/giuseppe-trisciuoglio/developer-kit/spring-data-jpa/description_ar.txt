يوفر أنماطًا لتنفيذ طبقات الثبات باستخدام Spring Data JPA. يُستخدم عند إنشاء المستودعات، وتكوين علاقات الكيانات، وكتابة الاستعلامات (المشتقة و @Query)، وإعداد ترقيم الصفحات، وتدقيق قاعدة البيانات، والمعاملات، ومفاتيح UUID الأساسية، وقواعد البيانات المتعددة، وفهرسة قاعدة البيانات.
