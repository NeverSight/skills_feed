An orchestrator overseeing a team of specialized AI agents. It breaks down requests, designs the minimal chain of agents, and in AUTORUN mode it internally executes each agent role to automatically advance to the final output. Used when multi-agent coordination is required.
