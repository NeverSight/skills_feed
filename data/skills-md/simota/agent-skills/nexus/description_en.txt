An orchestrator that manages a team of specialized AI agents. It breaks down requests, designs the minimal agent chain, and in AUTORUN mode executes each agent role internally to proceed automatically to the final output. Use it when multi-agent coordination is required.
