Genera e modifica immagini utilizzando il modello GPT Image 1.5 di OpenAI. Utilizzare quando l'utente richiede di generare, creare, modificare, cambiare, alterare o aggiornare le immagini. Da utilizzare anche quando l'utente fa riferimento a un file immagine esistente e chiede di modificarlo in qualsiasi modo (ad esempio, "modifica questa immagine", "cambia lo sfondo", "sostituisci X con Y"). Supporta la generazione di testo in immagine e la modifica delle immagini con maschera opzionale. NON leggere prima il file immagine: utilizza questa abilità direttamente con il parametro --input-image.
