إدارة حاويات PostgreSQL Docker للتطوير المحلي باستخدام أوامر pgpm docker. يُستخدم عندما يُطلب منك "بدء تشغيل postgres"، أو "تشغيل قاعدة البيانات محليًا"، أو "إعداد قاعدة بيانات محلية"، أو "إيقاف حاوية postgres"، أو عند الحاجة إلى قاعدة بيانات PostgreSQL للتطوير/الاختبار.
