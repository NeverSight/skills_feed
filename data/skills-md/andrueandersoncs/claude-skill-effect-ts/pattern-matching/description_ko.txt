이 스킬은 사용자가 "효과 일치", "패턴 일치", "Match.type", "Match.tag", "Match.when", "Schema.is()", "Schema.is with Match", "완전 일치", "차별화된 공용체", "Match.value", "전환을 일치로 변환", "if/else를 일치로 변환", "TaggedClass with Match"에 대해 질문할 때 사용해야 합니다. Effect가 유형이 안전한 철저한 패턴 일치를 제공하는 방법을 이해해야 합니다.
