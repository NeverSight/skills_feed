仅对外部系统写入（Jira、Confluence、BitBucket、Slack）强制执行确认工作流程。切勿激活线性操作（线性是内部跟踪）。当检测到对外部 PM 系统的潜在写入（状态更新、页面创建、PR 帖子、通知）时自动激活。阻止执行并显示将要写入的确切内容。需要明确的“是”确认（拒绝“好的”、“确定”、模棱两可的回答）。所有线性操作都会自动执行，无需确认。与 ccpm-code-review 一起工作，以确保外部广播之前的质量。提供所有已确认操作的审计跟踪。允许批量操作，并在需要时对每个项目进行细粒度确认。
