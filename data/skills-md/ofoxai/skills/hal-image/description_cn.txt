使用 ImageMagick (magick) 处理图像 — 读取元数据、调整大小、裁剪、旋转、转换格式、组合图像（并排或带有蒙太奇的网格）、覆盖徽标/水印，并在发送前使用 oxipng 进行无损压缩。当用户发送要处理的图像时、当您生成要传送的图像时或在将任何图像附加到消息或上传之前使用 - 首先压缩可以保持传输快速和令牌较低。核心原则 - 始终在交付之前进行压缩，绝不向用户公开本地文件路径，并且打开失败（如果工具丢失或步骤错误，则将原始文件不变地传递；绝不阻止任务）。
