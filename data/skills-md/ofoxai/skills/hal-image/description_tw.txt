使用 ImageMagick (magick) 處理圖像 — 讀取元資料、調整大小、裁剪、旋轉、轉換格式、組合圖像（並排或帶有蒙太奇的網格）、覆蓋徽標/水印，並在發送前使用 oxipng 進行無損壓縮。當使用者傳送要處理的影像時、當您產生要傳送的影像時或在將任何影像附加到訊息或上傳之前使用 - 首先壓縮可以保持傳輸快速和令牌較低。核心原則 - 始終在交付之前進行壓縮，絕不向用戶公開本地文件路徑，並且打開失敗（如果工具丟失或步驟錯誤，則將原始文件不變地傳遞；絕不阻止任務）。
