Manage global tool permission configuration for Claude Code, automatically adding MCP commands or other tools to allowedTools to avoid manual approval each time they are used. Workflow: confirm the command the user needs to add -> confirm the level of addition (default global ~/.claude.json) -> perform the addition -> verify and prompt to restart.
