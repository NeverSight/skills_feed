Manage Claude Code's global tool permission configuration, automatically adding MCP commands or other tools to allowedTools to avoid manual approval each time they are used. Workflow: confirm the command the user needs to add -> confirm the add level (default global ~/.claude.json) -> execute the add -> verify and remind to restart.
