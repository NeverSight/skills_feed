Search GitHub and automatically install and configure MCP (Model Context Protocol) server tools into the Claude configuration file. Trigger this skill when the user needs to install MCP tools. Workflow: search for MCP projects on GitHub → extract npx configuration → add to ~/.claude.json → handle API keys (if any).
