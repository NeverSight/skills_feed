Search GitHub and automatically install and configure MCP (Model Context Protocol) server tools into the Claude configuration file. This skill is triggered when the user needs to install an MCP tool. Workflow: search for MCP projects on GitHub → extract npx configuration → add to ~/.claude.json → handle API keys if any.
