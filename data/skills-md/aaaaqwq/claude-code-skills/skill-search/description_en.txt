Search for high-quality skills that match the user’s description on official sites like GitHub and SkillsMP, present them for selection, then automatically clone and install the chosen skill into the global ~/.claude/skills/ directory. Trigger this skill whenever the user needs to search for or install a new skill.
